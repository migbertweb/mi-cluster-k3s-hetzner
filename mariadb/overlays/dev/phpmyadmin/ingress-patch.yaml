apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: phpmyadmin-ingress
spec:
  rules:
    - host: mysql-local.migbertweb.xyz  # Dominio diferente
  tls:
  - hosts:
    - mysql-local.migbertweb.xyz